{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && npm install && npx tsx src/index.ts",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}